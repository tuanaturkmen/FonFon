server.address=0.0.0.0
server.port=${PORT:8080}


# --- DataSource config (PostgreSQL) ---
#spring.datasource.url=jdbc:postgresql://localhost:5432/fonfon_management
#spring.datasource.username=postgres
#spring.datasource.password=postgres
#spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:fonfon_management}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASS:postgres}
spring.cloud.gcp.sql.database-name=${DB_NAME}
spring.cloud.gcp.sql.instance-connection-name=${INSTANCE_CONNECTION_NAME}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
server.port=${PORT:8080}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# 2. RECOMMENDED: Control Schema Management
# Use 'validate' or 'none' for production/deployments where schema is managed manually.
# Use 'update' (carefully) if you want Hibernate to update tables automatically.
spring.jpa.hibernate.ddl-auto=validate

# --- JPA / Hibernate ---

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Allow CORS from frontend
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*


app.data.import.file-path=classpath:FundData.xlsx
app.data.import.enabled=false
